# Database
DATABASE_URL="postgresql://user:password@localhost:5432/mortgage_doc_ai?schema=public"

# Anthropic API Key (for agent LLM calls)
ANTHROPIC_API_KEY="sk-ant-..."

# Pipeline version (used in audit logs)
PIPELINE_VERSION="1.0.0"

# Set to "true" to enable deterministic test mode globally
DETERMINISTIC_MODE="false"
